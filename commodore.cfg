MEMORY {
    ZEROPAGE: start =   $00, size = $100, file = "";
    ROM:      start = $C000, size = $4000, file = %O;
}
SEGMENTS {
    ZEROPAGE: load = ZEROPAGE, type = rw, optional = yes;
    CODE:     load = ROM, align = 16, type = rw, define = yes;
}
